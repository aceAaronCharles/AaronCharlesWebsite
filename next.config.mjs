/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/AaronCharlesWebsite',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
